const lighthouses = ["Gibraltar Point", "Peggy's Point", "Cove Island", "Discovery Island", "Cape Scott", "Point Clark", "Kincardine"];

const mapFunc = function(arr) {
   return arr.map((word) => word.length )
}

console.log(mapFunc(lighthouses))

const tech = ["Women in Tech", "Javascript", "Shmoney", "Go Getters"];

const mapFunc2 = function(arr) {
   return arr.map((word) => word.length )
}

console.log(mapFunc2(tech))